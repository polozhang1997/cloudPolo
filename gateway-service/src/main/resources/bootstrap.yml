spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      discovery:
        locator:
          lower-case-service-id: true   #忽略服务大小写
          enabled: true             #动态创建路由
    nacos:
      discovery:
        server-addr: 10.165.12.109:8848
        enabled: true
      config:
        file-extension: yaml
        server-addr: 10.165.12.109:8848
        data-id: gateway-service-route
server:
  port: 1111